syntax = "proto3";

package order;

import "google/protobuf/empty.proto";
import "alpaca.proto";

option go_package = "github.com/sologenic/com-fs-order-model;order";

// Alpaca Trade log
service AlpacaService {
    rpc CreateAlpacaOrder(AlpacaOrderDetails) returns (google.protobuf.Empty) {}
  //  rpc Get(Filter) returns (Records) {}
    rpc UpdateAlpacaOrder(AlpacaOrderDetails) returns (google.protobuf.Empty) {} // Alpaca log line processed into order
}

