// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.181.2
//   protoc               v5.29.1
// source: attestation.proto
/* eslint-disable */
import Long from "long";
import _m0 from "protobufjs/minimal";
import { Timestamp } from "./google/protobuf/timestamp";
import { Decimal } from "./sologenic/com-fs-utils-lib/go/decimal/decimal";
import { networkFromJSON, networkToJSON } from "./sologenic/com-fs-utils-lib/models/metadata/metadata";
export const protobufPackage = "order";
function createBaseAttestation() {
    return {
        Network: 0,
        SmartContractAddr: "",
        Denom: "",
        BrokerQuantity: undefined,
        ChainQuantity: undefined,
        BlockHeight: 0,
        Timestamp: undefined,
        ReattestationBlockHeight: undefined,
    };
}
export const Attestation = {
    encode(message, writer = _m0.Writer.create()) {
        if (message.Network !== 0) {
            writer.uint32(8).int32(message.Network);
        }
        if (message.SmartContractAddr !== "") {
            writer.uint32(18).string(message.SmartContractAddr);
        }
        if (message.Denom !== "") {
            writer.uint32(26).string(message.Denom);
        }
        if (message.BrokerQuantity !== undefined) {
            Decimal.encode(message.BrokerQuantity, writer.uint32(34).fork()).ldelim();
        }
        if (message.ChainQuantity !== undefined) {
            Decimal.encode(message.ChainQuantity, writer.uint32(42).fork()).ldelim();
        }
        if (message.BlockHeight !== 0) {
            writer.uint32(48).int64(message.BlockHeight);
        }
        if (message.Timestamp !== undefined) {
            Timestamp.encode(toTimestamp(message.Timestamp), writer.uint32(58).fork()).ldelim();
        }
        if (message.ReattestationBlockHeight !== undefined) {
            writer.uint32(64).int64(message.ReattestationBlockHeight);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseAttestation();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    if (tag !== 8) {
                        break;
                    }
                    message.Network = reader.int32();
                    continue;
                case 2:
                    if (tag !== 18) {
                        break;
                    }
                    message.SmartContractAddr = reader.string();
                    continue;
                case 3:
                    if (tag !== 26) {
                        break;
                    }
                    message.Denom = reader.string();
                    continue;
                case 4:
                    if (tag !== 34) {
                        break;
                    }
                    message.BrokerQuantity = Decimal.decode(reader, reader.uint32());
                    continue;
                case 5:
                    if (tag !== 42) {
                        break;
                    }
                    message.ChainQuantity = Decimal.decode(reader, reader.uint32());
                    continue;
                case 6:
                    if (tag !== 48) {
                        break;
                    }
                    message.BlockHeight = longToNumber(reader.int64());
                    continue;
                case 7:
                    if (tag !== 58) {
                        break;
                    }
                    message.Timestamp = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
                    continue;
                case 8:
                    if (tag !== 64) {
                        break;
                    }
                    message.ReattestationBlockHeight = longToNumber(reader.int64());
                    continue;
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skipType(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            Network: isSet(object.Network) ? networkFromJSON(object.Network) : 0,
            SmartContractAddr: isSet(object.SmartContractAddr) ? globalThis.String(object.SmartContractAddr) : "",
            Denom: isSet(object.Denom) ? globalThis.String(object.Denom) : "",
            BrokerQuantity: isSet(object.BrokerQuantity) ? Decimal.fromJSON(object.BrokerQuantity) : undefined,
            ChainQuantity: isSet(object.ChainQuantity) ? Decimal.fromJSON(object.ChainQuantity) : undefined,
            BlockHeight: isSet(object.BlockHeight) ? globalThis.Number(object.BlockHeight) : 0,
            Timestamp: isSet(object.Timestamp) ? fromJsonTimestamp(object.Timestamp) : undefined,
            ReattestationBlockHeight: isSet(object.ReattestationBlockHeight)
                ? globalThis.Number(object.ReattestationBlockHeight)
                : undefined,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.Network !== 0) {
            obj.Network = networkToJSON(message.Network);
        }
        if (message.SmartContractAddr !== "") {
            obj.SmartContractAddr = message.SmartContractAddr;
        }
        if (message.Denom !== "") {
            obj.Denom = message.Denom;
        }
        if (message.BrokerQuantity !== undefined) {
            obj.BrokerQuantity = Decimal.toJSON(message.BrokerQuantity);
        }
        if (message.ChainQuantity !== undefined) {
            obj.ChainQuantity = Decimal.toJSON(message.ChainQuantity);
        }
        if (message.BlockHeight !== 0) {
            obj.BlockHeight = Math.round(message.BlockHeight);
        }
        if (message.Timestamp !== undefined) {
            obj.Timestamp = message.Timestamp.toISOString();
        }
        if (message.ReattestationBlockHeight !== undefined) {
            obj.ReattestationBlockHeight = Math.round(message.ReattestationBlockHeight);
        }
        return obj;
    },
    create(base) {
        return Attestation.fromPartial(base !== null && base !== void 0 ? base : {});
    },
    fromPartial(object) {
        var _a, _b, _c, _d, _e, _f;
        const message = createBaseAttestation();
        message.Network = (_a = object.Network) !== null && _a !== void 0 ? _a : 0;
        message.SmartContractAddr = (_b = object.SmartContractAddr) !== null && _b !== void 0 ? _b : "";
        message.Denom = (_c = object.Denom) !== null && _c !== void 0 ? _c : "";
        message.BrokerQuantity = (object.BrokerQuantity !== undefined && object.BrokerQuantity !== null)
            ? Decimal.fromPartial(object.BrokerQuantity)
            : undefined;
        message.ChainQuantity = (object.ChainQuantity !== undefined && object.ChainQuantity !== null)
            ? Decimal.fromPartial(object.ChainQuantity)
            : undefined;
        message.BlockHeight = (_d = object.BlockHeight) !== null && _d !== void 0 ? _d : 0;
        message.Timestamp = (_e = object.Timestamp) !== null && _e !== void 0 ? _e : undefined;
        message.ReattestationBlockHeight = (_f = object.ReattestationBlockHeight) !== null && _f !== void 0 ? _f : undefined;
        return message;
    },
};
function createBaseAttestations() {
    return { Attestations: [], Offset: undefined };
}
export const Attestations = {
    encode(message, writer = _m0.Writer.create()) {
        for (const v of message.Attestations) {
            Attestation.encode(v, writer.uint32(10).fork()).ldelim();
        }
        if (message.Offset !== undefined) {
            writer.uint32(16).int32(message.Offset);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseAttestations();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    if (tag !== 10) {
                        break;
                    }
                    message.Attestations.push(Attestation.decode(reader, reader.uint32()));
                    continue;
                case 2:
                    if (tag !== 16) {
                        break;
                    }
                    message.Offset = reader.int32();
                    continue;
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skipType(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            Attestations: globalThis.Array.isArray(object === null || object === void 0 ? void 0 : object.Attestations)
                ? object.Attestations.map((e) => Attestation.fromJSON(e))
                : [],
            Offset: isSet(object.Offset) ? globalThis.Number(object.Offset) : undefined,
        };
    },
    toJSON(message) {
        var _a;
        const obj = {};
        if ((_a = message.Attestations) === null || _a === void 0 ? void 0 : _a.length) {
            obj.Attestations = message.Attestations.map((e) => Attestation.toJSON(e));
        }
        if (message.Offset !== undefined) {
            obj.Offset = Math.round(message.Offset);
        }
        return obj;
    },
    create(base) {
        return Attestations.fromPartial(base !== null && base !== void 0 ? base : {});
    },
    fromPartial(object) {
        var _a, _b;
        const message = createBaseAttestations();
        message.Attestations = ((_a = object.Attestations) === null || _a === void 0 ? void 0 : _a.map((e) => Attestation.fromPartial(e))) || [];
        message.Offset = (_b = object.Offset) !== null && _b !== void 0 ? _b : undefined;
        return message;
    },
};
function toTimestamp(date) {
    const seconds = Math.trunc(date.getTime() / 1000);
    const nanos = (date.getTime() % 1000) * 1000000;
    return { seconds, nanos };
}
function fromTimestamp(t) {
    let millis = (t.seconds || 0) * 1000;
    millis += (t.nanos || 0) / 1000000;
    return new globalThis.Date(millis);
}
function fromJsonTimestamp(o) {
    if (o instanceof globalThis.Date) {
        return o;
    }
    else if (typeof o === "string") {
        return new globalThis.Date(o);
    }
    else {
        return fromTimestamp(Timestamp.fromJSON(o));
    }
}
function longToNumber(long) {
    if (long.gt(globalThis.Number.MAX_SAFE_INTEGER)) {
        throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
    }
    if (long.lt(globalThis.Number.MIN_SAFE_INTEGER)) {
        throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
    }
    return long.toNumber();
}
if (_m0.util.Long !== Long) {
    _m0.util.Long = Long;
    _m0.configure();
}
function isSet(value) {
    return value !== null && value !== undefined;
}
